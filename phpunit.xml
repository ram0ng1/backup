<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         executionOrder="depends,defects"
         beStrictAboutOutputDuringTests="true"
         failOnRisky="true"
         failOnWarning="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
    <php>
        <!-- Per-engine connection coordinates for the transfer tests.
             Unset engines are skipped, so the suite still runs (sqlite
             only) on a bare checkout. CI overrides these per matrix cell. -->
        <env name="BACKUP_TEST_MYSQL" value=""/>
        <env name="BACKUP_TEST_MARIADB" value=""/>
        <env name="BACKUP_TEST_POSTGRES" value=""/>
    </php>
</phpunit>
